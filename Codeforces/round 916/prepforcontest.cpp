#include <bits/stdc++.h>
using namespace std;

int dp[52][52][52];
int n, k;
vector<int>ans;
void rec(int level, int excited, int prev)
{
    if (level == n)
        return;
    
    //
}

void solve()
{
    cin >> n >> k;
}
signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    // int _t; cin>>_t;while(_t--)
    solve();
}